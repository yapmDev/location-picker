name: location_picker

environment:
  sdk: ^3.6.1
  flutter: ">=1.17.0"

dependencies:

  flutter:
    sdk: flutter

  flutter_map: ^6.0.0
  latlong2: ^0.9.0
  geolocator: ^13.0.2
  flutter_map_tile_caching: ^9.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0